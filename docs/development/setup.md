# Development Setup

> **Stub.** This document is filled in by MVP-001, Phase 8
> ([`../plans/MVP-001-workspace-foundation.plan.md`](../plans/MVP-001-workspace-foundation.plan.md)).
> Until then, the authoritative setup notes are in the repository root
> [`README.md`](../../README.md) and each component's own `README` /
> `readme.md`.

Intended contents:

- Prerequisites (.NET SDK, Node.js, `func`, Azure CLI, optional Cosmos DB
  emulator) with pinned versions.
- A verified "clone to running" block for each **active** component:
  - `source/judotech.api` (Azure Functions API)
  - `judotech-portal` (athlete app)
- A "clone to running" block for each **maintenance-only** component:
  - `source/judotech.web`, `source/judotech.web.calendar`,
    `source/judotech.web.club`, `source/judotech.web.referee`
- Which setup steps were verified, and on which operating system.
