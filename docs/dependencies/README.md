# Dependency Management

> **Stub.** This document is filled in by MVP-001, Phase 8
> ([`../plans/MVP-001-workspace-foundation.plan.md`](../plans/MVP-001-workspace-foundation.plan.md)).

Intended contents:

- How tool versions are pinned (`source/global.json`, `.nvmrc`,
  `packageManager`).
- How library versions are ranged, and where lockfiles are committed.
- Update cadence and how updates are reviewed.
- When adding or removing a dependency requires an ADR.
- Notes on dependencies removed or clarified during MVP-001 (`clean`,
  `@types/react-router-dom`, commented-out `judotech.core.csproj` package refs).
